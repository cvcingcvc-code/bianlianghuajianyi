# V2 前置审计 — 2026-09-22

基线 e37320e，分支 feature/minimal-trading-v1；242 tests / 229 pass / 13 fail。

## 已证实缺陷与现状

1. docs/PROJECT_STATUS.md 历史段称真实资金费验证通过，而 reports/funding-real-v1 的主报告、质量报告仍写 SYNTHETIC。不能以其为净优势证据；原报告保留，V2 重建独立来源链。
2. 旧 manifest 宣告2021–2025范围和5478行，bbc44c9提交显示重写BTC/ETH为6114行；脚本注释承认旧CSV含2026。预期SHA与实际文件不符已由基线测试证实。为遵守本轮边界，不读取这些混合年份CSV；不更改预期哈希。具体字节差异、换行影响尚未排除。
3. data/market 根目录缺少历史BTC/ETH/BNB/SOL CSV，原外部验证原始归档也缺少；价格文件被gitignore，历史报告引用部分不存在。缺失文件导致11个既有测试失败，另2个为资金费SHA不匹配。不得据此认定策略盈利或失败。
4. 默认 npm start 确实是 breakout24h4h、2025-01 BTC/ETH 回放；中文页标明历史。回放完成15秒后STALE、账户重启清空、缺少资金费和完整成本展示，均由源码证实。
5. SL/TP“仅存不执行”已在01d6f90修复，不是当前代码缺陷；27项V1测试包含next-bar、gap、双触、重复平仓及熔断。运行中的旧进程是否重启另需核实，不将旧进程行为当新代码证据。
6. 当前工作区已有 .gitattributes/package.json、trend-grid控制、paper-10u-15x等改动，本轮只显式暂存V2文件和状态文档。

## 待验证假设

- 资金费哈希错误可能由修复mark price/扩展行数后清单未同步及Windows换行共同造成；不得直接重写清单消错。
- 新4h模型是否优于无条件与趋势基准、能否覆盖成本、不同状态是否稳定，都须按预登记评估，不能从旧模型失败推断。

## 策略设计取舍

- 原突破策略只做多、均线退出、BTC/ETH共用仓位；不改其语义。
- 原趋势网格V1周预测失败、零交易是研究结果，不降低阈值救活。
- V2选择一个目标和一个简单模型、允许长期空仓、同根止损优先，是预先固定的保守取舍。

## 修复范围

V2独立实现持久化账户、回放完成状态、保护退出、成本分项和新报告来源链；不改旧页面、冻结策略和旧报告。旧13失败暂不修复：部分测试明确要求2026数据，与本轮边界不兼容；资金费混合文件需独立授权的数据修复阶段。新V2下载器只允许2021–2025。
